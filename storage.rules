rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Attachments storage - admin-only access
    match /attachments/{schoolId}/{noticeId}/{filename} {
      allow read, write: if request.auth.token.admin == true;
    }
  }
}